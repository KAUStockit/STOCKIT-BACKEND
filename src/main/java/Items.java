
public class Items {
    // code -> varchar(45), name(company) -> varchar(45), is_active -> boolean, issuance->double, url->varchar(45)
    private String code;
    private String name;
    private boolean is_active;
    private double issurance;
    private String url;
}
